name: Verify AI review environment

on:
  pull_request:
    paths:
      - .github/workflows/verify-ai-review-environment.yml

permissions: {}

jobs:
  verify:
    uses: aws/aws-durable-execution-ci/.github/workflows/verify-environment-secret.yml@codex/test-ai-env-secret
    secrets: inherit
