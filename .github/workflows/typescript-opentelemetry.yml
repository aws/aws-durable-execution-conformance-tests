name: TypeScript OpenTelemetry

on:
  pull_request:
    paths:
      - .github/workflows/typescript-opentelemetry.yml
      - .github/workflows/typescript-opentelemetry-suite.yml
      - .github/workflows/typescript-opentelemetry-long-running.yml
      - packages/aws-durable-execution-conformance-tests/src/**
      - packages/aws-durable-execution-conformance-tests-otel/src/**
      - packages/aws-durable-execution-conformance-tests-otel/test-requirements/otel-invocation/**
      - packages/aws-durable-execution-conformance-tests-otel/test-requirements/otel-execution/**
      - packages/aws-durable-execution-conformance-tests-otel/test-requirements/otel-long-running/**
      - packages/aws-durable-execution-conformance-tests-otel/collector/**
      - packages/aws-durable-execution-conformance-tests-otel/examples/typescript/**
  push:
    branches: [main]
    paths:
      - .github/workflows/typescript-opentelemetry.yml
      - .github/workflows/typescript-opentelemetry-suite.yml
      - .github/workflows/typescript-opentelemetry-long-running.yml
      - packages/aws-durable-execution-conformance-tests/src/**
      - packages/aws-durable-execution-conformance-tests-otel/src/**
      - packages/aws-durable-execution-conformance-tests-otel/test-requirements/otel-invocation/**
      - packages/aws-durable-execution-conformance-tests-otel/test-requirements/otel-execution/**
      - packages/aws-durable-execution-conformance-tests-otel/test-requirements/otel-long-running/**
      - packages/aws-durable-execution-conformance-tests-otel/collector/**
      - packages/aws-durable-execution-conformance-tests-otel/examples/typescript/**
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:
    inputs:
      phase:
        description: Run short tests, launch a long run, or check the active long run
        required: true
        default: short
        type: choice
        options:
          - short
          - launch
          - check
      delay_seconds:
        description: Durable delay in seconds (1-86400, launch only)
        required: true
        default: "82800"
        type: string
      aws_region:
        description: AWS region
        required: true
        default: us-west-2
        type: string

permissions:
  actions: write
  contents: read
  id-token: write

jobs:
  resolve-sdk-main:
    name: Resolve TypeScript SDK main
    runs-on: ubuntu-latest
    outputs:
      typescript_sdk_ref: ${{ steps.resolve.outputs.ref }}
    steps:
      - name: Resolve TypeScript SDK main commit
        id: resolve
        run: |
          TYPESCRIPT_SDK_REF=$(
            git ls-remote \
              https://github.com/aws/aws-durable-execution-sdk-js.git \
              refs/heads/main |
              awk '{print $1}'
          )
          if [ -z "$TYPESCRIPT_SDK_REF" ]; then
            echo "Could not resolve aws-durable-execution-sdk-js main"
            exit 1
          fi
          echo "Using TypeScript SDK main commit $TYPESCRIPT_SDK_REF"
          echo "ref=$TYPESCRIPT_SDK_REF" >> "$GITHUB_OUTPUT"

  invocation:
    name: Invocation view
    needs: resolve-sdk-main
    if: >-
      github.event_name != 'schedule' &&
      (github.event_name != 'workflow_dispatch' || inputs.phase == 'short')
    uses: ./.github/workflows/typescript-opentelemetry-suite.yml
    with:
      suite: otel-invocation
      case_count: 19
      aws_region: ${{ inputs.aws_region || 'us-west-2' }}
      typescript_sdk_ref: ${{ needs.resolve-sdk-main.outputs.typescript_sdk_ref }}
    secrets: inherit

  execution:
    name: Execution view
    needs: resolve-sdk-main
    if: >-
      github.event_name != 'schedule' &&
      (github.event_name != 'workflow_dispatch' || inputs.phase == 'short')
    uses: ./.github/workflows/typescript-opentelemetry-suite.yml
    with:
      suite: otel-execution
      case_count: 19
      aws_region: ${{ inputs.aws_region || 'us-west-2' }}
      typescript_sdk_ref: ${{ needs.resolve-sdk-main.outputs.typescript_sdk_ref }}
    secrets: inherit

  long-running-invocation:
    name: Long running invocation view
    needs: resolve-sdk-main
    uses: ./.github/workflows/typescript-opentelemetry-long-running.yml
    with:
      phase: >-
        ${{ (github.event_name == 'pull_request' || github.event_name == 'push')
          && 'short' || (github.event_name == 'schedule' && 'auto' || inputs.phase) }}
      view: invocation
      delay_seconds: >-
        ${{ (github.event_name == 'pull_request' || github.event_name == 'push'
          || inputs.phase == 'short')
          && '60' || (inputs.delay_seconds || '82800') }}
      aws_region: ${{ inputs.aws_region || 'us-west-2' }}
      typescript_sdk_ref: ${{ needs.resolve-sdk-main.outputs.typescript_sdk_ref }}
    secrets: inherit

  long-running-execution:
    name: Long running execution view
    needs: resolve-sdk-main
    uses: ./.github/workflows/typescript-opentelemetry-long-running.yml
    with:
      phase: >-
        ${{ (github.event_name == 'pull_request' || github.event_name == 'push')
          && 'short' || (github.event_name == 'schedule' && 'auto' || inputs.phase) }}
      view: execution
      delay_seconds: >-
        ${{ (github.event_name == 'pull_request' || github.event_name == 'push'
          || inputs.phase == 'short')
          && '60' || (inputs.delay_seconds || '82800') }}
      aws_region: ${{ inputs.aws_region || 'us-west-2' }}
      typescript_sdk_ref: ${{ needs.resolve-sdk-main.outputs.typescript_sdk_ref }}
    secrets: inherit
